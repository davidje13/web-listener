{
  "name": "web-listener-package-test",
  "private": true,
  "scripts": {
    "test": "./commonjs.cjs && ./es6modules.mjs && tsc --noEmit && lean-test && ./treeshake/run.sh"
  },
  "devDependencies": {
    "@types/ws": "8.x",
    "typescript": "6.0.x",
    "web-listener": "file:web-listener.tgz"
  }
}
